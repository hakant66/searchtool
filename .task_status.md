STATE: READY_FOR_REVIEW

Completed:
- Built initial Sold Item Finder codebase with Text Search, Email Conn, and .docx reporting.
- Added in-app query image crop tool and crop-based image similarity search flow.
- Integrated optional OpenAI semantic pipeline (vision description, embeddings storage, hybrid ranking, and UI toggles) with fallback mode.
- Added Settings tab toggle to enable/disable OpenAI embedding generation at runtime (persisted locally).
- Updated crop flow to preview first, then save cropped query images under project temp directory.
- Added Settings tab "Test OpenAI Connection" button with background verification and status updates.
- Added .env-based OpenAI key loading support with template and docs.
- Improved image matching reliability (16x16 dHash, bit-level Hamming, and safer semantic blend weighting).
- Added image-search debug score panel showing top candidate pHash/embedding/final score components.
- Fixed IMAP fetch parser bug (`email.policy` import) that blocked email retrieval.
- Added strict exact SHA-only image search mode with low-similarity warnings in UI.
- Added GUI model visibility for search and optional Email Conn debug screen toggle.
- Improved Gmail email fetch robustness with reliable header parsing and recipient filter fallback.
- Enhanced visual-only image ranking with pixel-level rerank on top hash candidates.
- Migrated Email Conn to a local MCP email server tool with subject/body parsing and PDF/DOCX/CSV/XLSX attachment text extraction.
- Implemented staged vision-embedding rerank: vision table + index-time generation + cosine rerank; kept gpt-4o as UI explainability only.
- Added Email Conn attachment viewer pane with structured attachment extraction output (including PDF text) per selected email.
